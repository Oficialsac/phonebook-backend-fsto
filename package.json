{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build:iu": "rm -rf build && mkdir build && cd ../phonebook-fullstackopen && npm run build && cp -r build ../part3-a",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "12.0.3",
    "express": "4.17.2",
    "mongoose": "6.1.6",
    "mongoose-unique-validator": "3.0.0",
    "morgan": "1.10.0"
  },
  "devDependencies": {
    "nodemon": "2.0.15"
  }
}
